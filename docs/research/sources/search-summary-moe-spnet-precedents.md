I found a small but coherent set of papers around this design space. The closest direct precedents are **MoE-SPNet**, which adaptively reweights hierarchical features at each spatial location, and **Learning Dynamic Routing for Semantic Segmentation**, which selects scale-transform paths from the input’s scale distribution. The strongest architectural match to hierarchical independent routing is **HoME**, which separates local-group expert routing from global expert refinement in 3D segmentation. More recent work shifts specialization toward patches, tasks, morphology, or object shape: patch-wise sparse CNN MoE provides evidence on local routing and reports gains up to 3.9 mIoU; Semi-MoE uses independent segmentation, distance-field, and boundary experts; and ShapeMoE routes objects by latent shape characteristics. Together, these papers suggest a useful research direction: separate experts by resolution or receptive-field regime, route independently at local regions or tokens, and retain a shared/global path to prevent fragmented representations. The table distinguishes direct multi-scale precedents from adjacent designs rather than treating all MoE papers as equivalent.